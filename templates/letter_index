{% extends 'base.html' %}

{% block content %}

<div class="header">
    <div>
        <h1 class="index-title"> {{ letter }} </h1>
    </div>
    <div>
        <button><a href="/index">GO BACK</a></button>
    </div>
</div>

<div class="column-layout index-container">
    {% for data in list %}
        <div class="grid-item">
            <a href="/index/{{data.id}}"> <i>{{ data.species }}</i></a>
        </div>
    {% endfor %}
</div>

{% endblock %}