{% extends 'base.html' %}

{% block content %}

<h1>MAIN INDEX</h1>
<div class="column-layout">
    {% for data in list %}
        <div class="grid-item">
            <a href="/index/{{data.id}}">{{ data.species }}</a>
        </div>
    {% endfor %}
</div>

{% endblock %}